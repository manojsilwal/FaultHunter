{
  "name": "faulthunter-browser",
  "private": true,
  "type": "module",
  "scripts": {
    "browser:install": "playwright install chromium",
    "browser:probe": "node browser/probe.mjs"
  },
  "devDependencies": {
    "playwright": "^1.53.1"
  }
}
